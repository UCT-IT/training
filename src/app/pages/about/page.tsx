import History from '@/app/components/about/history/History';
import React from 'react';

const page = () => {
    return (
        <React.Fragment>
            <History />
        </React.Fragment>
    );
};

export default page;